services:
  duh:
    image: ghcr.io/justinpopa/duh:latest
    network_mode: host
    restart: unless-stopped
    volumes:
      - duh-data:/data
    command: ["--data-dir", "/data", "--proxy-dhcp"]

volumes:
  duh-data:
